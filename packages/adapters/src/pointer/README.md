# Pointer Adapter

Provides DOM target and surface-level pointer binding for URK.

## Requires

- no required host service

## Exposes

- `bindTarget(...)`
- `bindSurface(...)`
- `clear()`

## Example

- `examples/audio-proof/`
- `examples/picking/`
- `examples/app-shell/`
- `examples/scrollytelling/`
